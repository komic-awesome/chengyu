import React, { Component, PropTypes } from 'react'
import { green400 } from 'material-ui/styles/colors';

let styles = {
  height: 400,
  background: green400,
}

class OptionsPageHeader extends Component {
  render() {
    return (
      <div style={styles} />
    )
  }
}

export default OptionsPageHeader
